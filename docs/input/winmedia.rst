WinMedia
=======

On Microsoft Windows, various programs will publish to the system what media is playing.  The `WinMedia` option will read that data as the Source. There are no extra options, other than to select it.

